from shopping_cart import ShoppingCart

class Customer:

    def __int__(self, name, cart_passed_in):
        self.name = name
        self.shopping_cart = cart_passed_in

    def add_product_to_cart(self, add_items_to_cart()):
        #Still working on this 
